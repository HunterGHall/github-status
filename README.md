# github-statues

Set your GitHub status via GraphQL.

## Setup

Put your GitHub personal access token (with the `user` scope) in `token.txt`.

```
pip install requests
```

## Usage

```
python main.py
```
